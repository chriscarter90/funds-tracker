<% flash.keys.each do |key| %>
  <div class="alert <%= bootstrap_class_for(key) %> alert-dismissable">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
    <%= flash[key] %>
  </div>
<% end %>
