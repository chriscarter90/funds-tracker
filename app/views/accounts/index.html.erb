<h3>Accounts</h3>

<% if @accounts.any? %>
  <ul>
    <% @accounts.each do |a| %>
      <li>
        <%= a.name %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>
    You have no accounts. Try making one <%= link_to "here", new_account_path %>.
  </p>
<% end %>
